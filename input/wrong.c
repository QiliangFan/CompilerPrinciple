int fun(int c)
{
    print(c);
}

//错误：main的参数列表漏掉了
int main()
{

    // int a = 1.2;

    //错误：语句后使用了冒号而非分号
    int a, b = 1 :

        //错误：变量未定义
        // a = c + 10;

    // //错误：变量已定义   
    int a = b;

    // //错误：传参个数不匹配
    // fun();

    // //错误：函数未定义
    fuc();

    // //错误：传入参数为空
    // print();

    // //错误：return 语句漏掉了分号
    return 0;
}
